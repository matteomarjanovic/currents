{
  "lexicon": 1,
  "id": "is.currents.feed.findSimilarInLibrary",
  "defs": {
    "main": {
      "type": "query",
      "description": "Find the authenticated viewer's own saved images visually similar to a source save, optionally scoped to specific collections. Requires authentication.",
      "parameters": {
        "type": "params",
        "required": [
          "uri"
        ],
        "properties": {
          "uri": {
            "type": "string",
            "format": "at-uri",
            "description": "AT-URI of the source save whose image seeds the similarity search."
          },
          "collections": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "at-uri"
            },
            "description": "AT-URIs of collections to narrow the search to. If omitted, searches the viewer's entire library."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 50
          },
          "cursor": {
            "type": "string"
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "saves"
          ],
          "properties": {
            "cursor": {
              "type": "string"
            },
            "saves": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "is.currents.feed.defs#saveView"
              }
            }
          }
        }
      }
    }
  }
}
