{
  "lexicon": 1,
  "id": "is.currents.feed.getSaves",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get saves within a collection, hydrated with CDN image URLs and metadata.",
      "parameters": {
        "type": "params",
        "required": [
          "collection"
        ],
        "properties": {
          "collection": {
            "type": "string",
            "format": "at-uri",
            "description": "AT-URI of the collection record to fetch saves from."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 50
          },
          "cursor": {
            "type": "string"
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "saves"
          ],
          "properties": {
            "cursor": {
              "type": "string"
            },
            "saves": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "is.currents.feed.defs#saveView"
              }
            }
          }
        }
      }
    }
  }
}