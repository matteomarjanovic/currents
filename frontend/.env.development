# Committed, don't add any secrets here.
# Development backend — used by `npm run dev`, `dev:android`, `dev:ios` (vite mode=development).
PUBLIC_APPVIEW_URL=https://api-dev.currents.is

# Preview gate off in dev — everyone sees organize + supporter UI.
PUBLIC_PREVIEW_FEATURES=

# Paddle sandbox (supporter tier). The price vars take the pri_... ids from the
# sandbox catalog (Catalog > Products > Currents Supporter > each price), not
# dollar amounts.
PUBLIC_PADDLE_ENVIRONMENT=sandbox
PUBLIC_PADDLE_CLIENT_TOKEN=test_4308389ce59d8d398de02acf44c
PUBLIC_PADDLE_PRICE_MONTHLY=pri_01kwgxfvbqb6pxnf43e4d4ees8
PUBLIC_PADDLE_PRICE_YEARLY=pri_01kwgxh1htw9b6pd5tw731ah66
