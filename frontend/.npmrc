engine-strict=true

# Capawesome Insiders private registry — required for @capawesome-team/* packages.
# Set CAPAWESOME_TOKEN in your environment (do not commit secrets).
# Get a license at https://capawesome.io/insiders
@capawesome-team:registry=https://npm.registry.capawesome.io/
//npm.registry.capawesome.io/:_authToken=${CAPAWESOME_TOKEN}
